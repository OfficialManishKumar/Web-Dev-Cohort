// We use "git init" keyword to allow git to track our current folder
// And after that we can see any of sybol among these three in our file:
// - "U" = Untracker = Not able or permitted to track that file
// - "A" = Index Added = git had tracked that file and changes in that file had to be saved. We can use `git add ${filepath}` keyword to make any file indexed or tracked or the keyword "git add ." to make all the files of the repository indexed or tracked
// - 